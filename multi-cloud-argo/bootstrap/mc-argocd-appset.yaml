apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: mc-argocd-install
  namespace: openshift-gitops
spec:
  generators:
  - clusterDecisionResource:
      labelSelector:
        matchLabels:
          cluster.open-cluster-management.io/placement: multicloud-placement
      requeueAfterSeconds: 180
  template:
    metadata:
      name: '{{name}}-argocd-install'
      annotations:
        apps.open-cluster-management.io/ocm-managed-cluster: '{{name}}'
        apps.open-cluster-management.io/ocm-managed-cluster-app-namespace: openshift-gitops
        argocd.argoproj.io/skip-reconcile: "true"
      labels:
        apps.open-cluster-management.io/pull-to-ocm-managed-cluster: "true"
    spec:
      project: default
      destination:
        server: https://kubernetes.default.svc
        namespace: openshift-operators
      source:
        repoURL: https://github.com/purefield/multi-cloud-demo.git
        targetRevision: argocd
        path: multi-cloud-argo/bootstrap/operator
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
