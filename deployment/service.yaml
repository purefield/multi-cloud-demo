apiVersion: v1
kind: Service
metadata:
  name: helloworld-app-svc
  namespace: hello
  labels:
    app: helloworld-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: helloworld-app
